essentialy
if statements that allow
for manipulation of data

{
    {
        slugify
    }
}
handlebars helper.Uses the awesome underscore.string to transform text into a URL slug.Replaces whitespaces, accentuated and special characters with a dash.

//STORE IN UTILS FOLDER